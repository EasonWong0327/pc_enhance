在理想情况下，模型的输出应该与输入点数一致，即如果输入是形状为 (1000, 3) 的点云块，输出也应该是 (1000, 3)。然而，出现不一致的情况可能是由以下几种原因造成的：

稀疏张量处理中的坐标重复：如果在创建稀疏张量时，输入的坐标中有重复点，那么稀疏卷积会合并这些重复的坐标，从而导致输出点数少于输入点数。
模型设计中的下采样层：某些模型（例如 U-Net 结构）会在编码阶段对输入进行下采样。这种下采样导致输出点数减少。虽然最终解码器通常会将点数恢复，但在中间层输出时可能会发生不匹配。
稀疏卷积的操作特性：在一些稀疏卷积网络中（例如 MinkowskiEngine），如果输入中有一些点在特定操作（如池化或卷积）中没有参与计算，输出可能会出现点数减少的情况。
模型的特定实现：有些模型在实现时可能没有严格保持输入输出一致，比如有些层会在边缘点进行裁剪或过滤，从而导致输出少于输入点。

为了确认问题来源，你可以逐步检查以下几点：
检查输入数据的唯一性：在将点云输入到模型之前，确保 normalized_coords_B 中没有重复的点。
观察模型结构：确认模型中是否有下采样层或其他可能导致点数减少的操作。
逐层调试输出：通过打印每层的输出点数，找到第一个导致点数减少的层。这样可以帮助定位是哪个具体层引起了不一致。

根据这些检查结果，你可以考虑以下改进方法：
移除重复点（如有必要），确保每个输入块在稀疏张量创建时是唯一的。
如果模型需要严格保持输入输出一致，可以尝试修改网络结构，使其避免下采样或进行其他改变。
